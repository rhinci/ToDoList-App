<Window x:Class="ToDoList.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ToDoList"
        mc:Ignorable="d"
        Title="ToDoList" Height="450" Width="800">
    <Grid x:Name="MainGrid" Background="#fcedd3" >
        <Grid.RowDefinitions>
            <RowDefinition Height="35*" />
            <RowDefinition Height="383*" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="537*" />
            <ColumnDefinition Width="263*" />

        </Grid.ColumnDefinitions>

        <Frame Grid.Column="1" Grid.Row="0" Grid.RowSpan="2" Background="#015185"></Frame>
        <TextBlock Grid.Column="1" FontSize="30" FontFamily="Britannic Bold" VerticalAlignment="Center" TextAlignment="Center" Foreground="#cea273" Height="33">Categories</TextBlock>

        <DataGrid Grid.Column="0" Grid.Row="0"  Grid.RowSpan="2" Background="#fcedd3" Margin="10,10,10,10" ItemsSource="{Binding Tasks}">
            <DataGrid.Columns>
                <DataGridTemplateColumn Header="Is Done">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <RadioButton IsChecked="{Binding IsCompleted}" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="Title" Binding="{Binding Title}" />
                <DataGridTextColumn Header="Category" Binding="{Binding Category}" />
                <DataGridTextColumn Header="Description" Binding="{Binding Description}" />
                <DataGridTextColumn Header="Date" Binding="{Binding DueDate}" />
                <DataGridTextColumn Header="Priority" Binding="{Binding Priority}" />
            </DataGrid.Columns>
        </DataGrid>

    </Grid>




</Window>
